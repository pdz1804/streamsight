"""Video ingest and the WebSocket transport that carries annotated frames out.

Capture and source registration on the way in, the producer/consumer pump in
:mod:`app.streaming.session`, and the binary frame format in
:mod:`app.streaming.wire` on the way out.
"""
